name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    container: node:18
    services:
      db:
        image: postgres:15
    steps:
      - uses: docker://node:18
      - run: echo "test"
  test2:
    runs-on: ubuntu-latest
    container:
      image: redis:7
      options: --user root
    steps:
      - run: echo "test"
